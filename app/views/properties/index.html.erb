<h2>List of Buildings</h2>
<h3><%= notice %></h3>
<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>City</th>
      <th>State</th>
      <th>Zip Code</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @property.each do |property| %>
      <tr>
        <td><%= property.address %></td>
        <td><%= property.city %></td>
        <td><%= property.state %></td>
        <td><%= property.zip %></td>
        <td><%= property.description %></td>
      </tr>
    <% end %>
  </tbody>

</table>
<br>
<h3><%= notice %></h3>
<%= link_to 'Add Building', new_property_path %> |
 <%= link_to 'Add Owner', new_owner_path %>
